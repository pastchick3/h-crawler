use crate::crawler::Crawler;
use std::path::PathBuf;

pub fn crawl_user(crawler: Crawler, output: PathBuf, ids: Vec<String>) {

}

pub fn crawl_artwork(crawler: Crawler, output: PathBuf, ids: Vec<String>) {

}